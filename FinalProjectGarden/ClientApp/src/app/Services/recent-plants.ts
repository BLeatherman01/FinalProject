export interface RecentPlants {
    id : number, 
    plantDate? : Date,
    pickBloom? : Date,
    wateringFreq? : number,
    season? : string,
    plantId : string,
    plantImageUrl : string,
    // googleId : string
    gardenId : number,
    
}
